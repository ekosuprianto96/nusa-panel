# NusaPanel Environment Configuration
# PENTING: Jangan commit file .env ke repository!

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
DATABASE_URL=mysql://root:password@localhost:3306/nusa_panel

# ===========================================
# JWT CONFIGURATION
# ===========================================
# Secret key untuk JWT - GANTI DENGAN VALUE YANG AMAN DI PRODUCTION!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
# Token expiration dalam detik (default: 24 jam)
JWT_EXPIRATION=86400
# Refresh token expiration dalam detik (default: 7 hari) 
JWT_REFRESH_EXPIRATION=604800

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
# Mode aplikasi: development, staging, production
RUST_ENV=development
# Log level: trace, debug, info, warn, error
RUST_LOG=debug

# ===========================================
# SECURITY CONFIGURATION
# ===========================================
# Allowed origins untuk CORS (comma separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
# Rate limiting: requests per minute
RATE_LIMIT_PER_MINUTE=60
# Password minimum length
PASSWORD_MIN_LENGTH=8

# ===========================================
# FILE MANAGEMENT
# ===========================================
# Base path untuk user home directories
USER_HOME_BASE=/home
# Maximum upload size dalam bytes (default: 100MB)
MAX_UPLOAD_SIZE=104857600

# ===========================================
# PHPMYADMIN CONFIGURATION
# ===========================================
PHPMYADMIN_URL=http://localhost:8080/phpmyadmin
